[build]
builder = "nixpacks"

[deploy]
startCommand = "npx sequelize-cli db:migrate && npx sequelize-cli db:seed:all && npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
